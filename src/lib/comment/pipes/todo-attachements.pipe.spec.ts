import { TodoAttachementsPipe } from './todo-attachements.pipe';

describe('TodoAttachementsPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoAttachementsPipe();
    expect(pipe).toBeTruthy();
  });
});
